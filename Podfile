platform :ios, '10.0'
use_frameworks!

# ignore all warnings from all pods
inhibit_all_warnings!

target 'AniLog' do

  # Pods for AniLog
  pod 'Reusable'
  pod 'Alamofire', '~> 5.5'
  pod 'Toast-Swift', '~> 5.0.1'
  pod 'PopupDialog', '~> 1.1'
  pod 'PanModal'

  target 'AniLogTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AniLogUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
